---
"ironprose-cli": minor
---

feat: add `--output markdown` format for analyze and compare commands

Generates self-contained Markdown reports suitable for sharing and archiving. Analyze reports group diagnostics by severity with telemetry metadata. Compare reports summarize fixed/persistent issues by rule count and list introduced issues individually with line references.
